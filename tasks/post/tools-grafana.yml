---
- name: Grafana tooling install is managed by Homebrew
  ansible.builtin.debug:
    msg:
      - "Grafana tooling (e.g., loki/logcli/promtail, mimirtool, grizzly) is now installed via Homebrew."
      - "See `homebrew_installed_packages` in `default.config.yml` (or override via `config.yml`)."
