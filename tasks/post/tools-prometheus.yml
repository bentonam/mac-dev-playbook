---
- name: Prometheus tooling install is managed by Homebrew
  ansible.builtin.debug:
    msg:
      - "Prometheus tooling (e.g., prometheus/promtool, alertmanager/amtool, promlens, exporters) is now installed via Homebrew."
      - "See `homebrew_installed_packages` in `default.config.yml` (or override via `config.yml`)."
